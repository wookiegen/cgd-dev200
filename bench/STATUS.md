# STATUS.md — where the benchmark stands (2026-09-10)

What exists on the group server, what is running unattended, what remains. Paths are under `/data/wookiekim/cgd/data/bench/` (`$CGD_BENCH_ROOT`) unless noted; results are in this repo under `results/bench/`; logs and completion markers in `/data/wookiekim/cgd/data/_logs/`. Raw data and model weights: `DATA.md`. The paper's tables are filled from `results/bench/SUMMARY.md` (regenerate with `python bench/assemble.py`).

## 1. Generated data on the server

| What | Path | State |
|---|---|---|
| Clean latents of the real crops (decoder-only reference rows) | `latents/ref/<split>/<id>.pt` | 12,000 done (multigen5k, ade20k_val2k, coco_val5k) |
| VAE round trip (512) | `outputs/ref/vae_roundtrip/<split>/<id>.png` | done |
| vanilla PiD round trip (2048 + 512 view) | `outputs/ref/pid_roundtrip/<split>/`, `outputs/ref/pid_roundtrip_512/<split>/` | done |
| OminiControl latents (x_0 + x_t at 16, 24) and its VAE decode | `latents/omini/<canny,depth>/<id>.pt`, `outputs/omini/<cond>/vae@28/` | done (5000 + 5000) |
| vanilla PiD decodes of the OminiControl latents at K = 28 / 24 / 16 | `outputs/omini/<cond>/pid@<K>/`, `pid@<K>_512/` | done + SCORED (block filled in the paper 2026-09-11) |
| EasyControl latents (canny, depth; seg on ade20k_val2k) and FLUX ControlNet latents (canny, depth) | `latents/<easycontrol,fluxcn>/<cond>/`, `outputs/<ctrl>/<cond>/vae@28/` | done (all five caches) |
| their PiD decodes at K | `outputs/<ctrl>/<cond>/pid@<K>/` | fluxcn: done + SCORED (block filled in the paper 2026-09-12 02:30 KST; canny 0.656 VAE / 0.655 PiD K=24, depth RMSE 29.9 / 29.9, FID 16.5 / 16.6); easycontrol: done + SCORED (block filled in the paper 2026-09-12 06:00 KST; canny 0.810 VAE / 0.802 PiD K=24, depth RMSE 22.5 / 22.3, seg mIoU 39.3 / 39.6, FID 20.9 / 18.2) |
| CGD training triplets | `train/<set>/` | NOT built (`make_targets.py`; measured 2026-09-12: 1.3 s/row at `--batch 8`, so ~174k rows = ~60 GPU-hours, ~16 h on 4 GPUs; 24 val rows of multigen_train30 exist from the timing run) |

**2026-09-12 evening (user decisions; details in `update.md` Sections 7-8 and BENCHMARK v1.10 / v1.11):** 2048 is the reference resolution, the 512 view is derived (option 1: cached latents kept; native condition = Canny of the PiD round trip, `multigen5k/conditions/canny2048`; baselines scored, `*.cond2048*.json`); the NATIVE-ROUTE baseline (controllers generating at 2048 directly, subset500) is QUEUED and red in the paper; PiD in TWO forms: student `pid_k<K>` (main tables) + undistilled TEACHER `pidt_k<K>` (25 steps, CFG 5, `decode_pid.py --pid-ckpt-type teacher`), compared on subset500 in the paper's `tab:teacher`; the teacher block (`run/run_teacher_block.sh`, marker `done_teacher_block.txt`, ~25 GPU-h) is queued by `/data/wookiekim/cgd/tmp/teacher_after_all.sh` to start on all four GPUs once the 1024 side comparison and both conditioned-VAE blocks finish (~17:40 KST). Environment note: torch was accidentally upgraded by a plain `pip install` and restored to 2.5.1+cu121 the same hour; always `pip install --no-deps` here.

**2026-09-12: the dev-200 loop's gate moved to the TOLERANT canny F1 (one condition pixel: 1 px @512, 4 px @2048) at both resolutions; strict F1 stays as the 512 anchor** (`scripts/03_score.py` reports both; README + `DEV_SETTING.md` tables rescored on the same outputs). Anyone on the first commit: `git pull` and re-run `scripts/03_score.py`. The scoring rule across resolutions is written in the paper (`subsubsec:exp-resolution-protocol`) and BENCHMARK v1.9.1; control numbers in `results/bench/metric_checks/`.

**2026-09-12 (user): bounding-box layout DROPPED from the paper.** `coco_val5k`, `train/coco_train`, the GroundingDINO scorer, and `results/bench/coco_val5k/` stay in the repo unused (measured for the record: layout SR 72.7 real / 72.1 VAE round trip / 70.9 PiD round trip, so the decoder barely moves it). The COCO 2017 data on disk (`/data/wookiekim/cgd/data/coco2017`, 39 GB) is kept for now and is to be DELETED after the submission is complete.

Completion markers: `done_ref_all.txt`, `done_latents_omini_all.txt`, `done_omini_block.txt`, `done_latents_<ctrl>_<cond>.txt`, `done_easycontrol_block.txt`, `done_fluxcn_block.txt`, `done_queue.txt`. Status lines: `omini_block_status.txt`, `queue_status.txt`, `<ctrl>_block_status.txt`.

## 2. Results in the repo (`results/bench/`)

| File | Content |
|---|---|
| `<split>/[<controller>.]<method>@<res>[.<metrics>][.<condition>].json` | one record per condition: adherence + quality (schema: paper repo `SCORING_HARNESS.md`). The controller prefix (e.g. `omini.vae@512.canny.json`) keeps the three controller blocks apart; `.<metrics>` marks metric-subset runs (e.g. `.vlm`); `.<condition>` marks runs restricted to one condition of the split. `assemble.py` merges everything by (split, controller, method, res, condition) |
| `<split>/<method>@<res>_per_image.csv` | per-sample scores (paired analysis, figures) |
| `efficiency.json` | tab:efficiency measurements (one H200) |
| `SUMMARY.md`, `summary.csv` | everything merged, one row per (split, condition, method, controller, res) |

Done: `real`, `vae_roundtrip`, `pid_roundtrip` on all three splits at 512 (and 2048 for PiD), incl. all eight no-reference metrics on multigen5k. Method labels for the controller blocks: `vae` (the controller's VAE decode, `--controller <ctrl>`), `pid_k28`, `pid_k24`, `pid_k16`.

## 3. Findings so far

- **Edge metric changed to tolerant F1 (BENCHMARK v1.8, 2026-09-11)**: strict F1 at 2048 compared 1-2 px re-extracted edges with the 4-px-thick nearest-resampled reference (a bicubic x4 of the REAL image scores 0.149 strict), so the strict 0.64 -> 0.26 drop of the PiD round trip was mostly the metric. Paper metric = F1 with a one-condition-pixel tolerance (1 px @512, 4 px @2048); strict kept as `f1_strict`. Tolerant: VAE round trip 0.94 @512; PiD round trip 0.92 -> 0.79; OminiControl + VAE decode 0.76; + PiD K=28 0.78 -> 0.73; K=16 0.54 -> 0.56. `bench/rescore_canny.py` patched the existing records.
- Deterministic decode ceiling for strict canny F1 at 512: 0.747 (VAE round trip; 0.94 tolerant); segmentation and layout are unchanged by the decode (53.6 / 72.1 vs 53.7 / 72.7 on the real images). Depth / seg / layout scorers resize internally, so their 2048 values are flat by construction (moved to a supplementary table).
- vanilla PiD on the same clean latent: canny 0.637 at the 512 view and 0.256 at native 2048 (the resolution gap), depth MSE 62 vs 11 for the VAE, seg / layout unchanged, MANIQA higher (0.58 vs 0.48): the blind generative decoder invents fine structure.
- FLUX + PiD reaches 2048 in 2.4 to 3.3 s vs 36 s for native FLUX + VAE; native FLUX at 4096 is out of memory on an H200, FLUX-at-1024 + PiD takes 12 to 15 s.
- 512-generation behavior of EasyControl and FLUX ControlNet Union-Pro-2.0 verified visually (both follow the condition; EasyControl's seg LoRA reads the ADE20K palette render).

- **OminiControl block (2026-09-11, tolerant F1 512 | 2048 / depth RMSE / FID):** VAE decode 0.771 | n/a / 23.8 / 17.2; vanilla PiD K=28 0.785 | 0.733 / 23.5 / 16.1; K=24 0.719 | 0.669 / 23.6 / 18.1; K=16 0.548 | 0.572 / 30.8 / 26.3. Truncation is expensive for the blind decoder (edges and, at K=16, depth) while MANIQA rises 0.62 -> 0.68: it fills the missing steps with detail that scores well and follows the condition less.

## 3b. How to fill a controller block into the paper once its marker exists

1. In the container: `cd /data/wookiekim/cgd/cgd-dev200/bench && python tools_rename_controller_results.py && python assemble.py && python collect_block.py <controller> --out /tmp/<controller>_numbers.json` (prints every number of the block: `vae@512`, `pid_k28/24/16@512`, `pid_k*@2048`, with canny F1 (tolerant) + f1_strict, depth MSE/RMSE, seg mIoU where present, FID/pFID, the 8 no-ref metrics, PSNR/SSIM/LPIPS/DISTS).
2. Paper repo `docs/EXPERIMENTS_draft.tex`: fill the block's `+ VAE decode` and `+ PiD` rows of `tab:main` (PiD row shows K = 24 while K is undecided; put K = 28 and 16 in the `%` comment), the `<controller> + PiD` row of `tab:native-other`, and, for EasyControl, the seg cell (ade20k_val2k) and the choice in OPEN_QUESTIONS 13; extend the modularity paragraph with the per-controller VAE vs PiD deltas. Keep the `%` provenance comment convention (result file names).
3. `python3 tools/table_widths.py` in the paper repo; `git add results/bench bench/STATUS.md && git commit && git push` here; update the table in Section 1 above.

Running at hand-off (2026-09-12 15:25 KST, detached inside the container): (a) **condition-scale sweep DONE 15:17 KST** (`results/bench/sweep/condition_scale.json` + `SUMMARY.md`; fig:ceiling rendered by `plot_ceiling.py` into the paper repo's `figures/ceiling.pdf`; paragraph written): VAE decode 0.25 / 0.25 / 0.40 / 0.67 / 0.77 / 0.84 / 0.86 / 0.84 at scale 0 / 0.25 / 0.5 / 0.75 / 1 / 1.5 / 2 / 4 vs the round-trip ceiling 0.94 on the same 200 images; PiD K=24 512 | 2048 = 0.71 | 0.66 at scale 1 and 0.78 | 0.72 at scale 2; the conditioned-VAE trainings started right after on GPUs 0 (canny) and 1 (depth), markers `done_condvae_<cond>.txt`; (b) **DreamBench subject track** (user order 2026-09-12: sweep, then subject rows, then the conditioned VAE decoder; the CGD training targets are POSTPONED): the 50-image prompt A/B on GPU 3 found no difference (class-name prompt: DINO 0.505 / CLIP-I 0.790 / CLIP-T 0.255; "this item" phrasing: 0.501 / 0.787 / 0.259; backpack + backpack_dog x 25 prompts), so the DreamBench class-name prompt is kept; `run/run_subject_block.sh omini` launched 13:35 KST on GPUs 2,3 (generation ~9.7 s per image with image guidance, ETA ~15:05 KST incl. decode + scoring; marker `done_subject_omini.txt`). (c) Conditioned VAE decoder: `cond_vae.py` (FLUX VAE decoder + zero-init condition branch; equals the VAE at init, verified), `train_cond_vae.py`, `precompute_real_depth.py`, `decode_cond_vae.py`, `run/run_cond_vae.sh <canny|depth>`; training crops (36,622, `images512` + canny of the source) and real-crop depth conditions (`conditions_real/depth`, 36,622) are DONE; a detached launcher (`/data/wookiekim/cgd/tmp/condvae_after_sweep.sh`) waits for `done_sweep_condition_scale.txt` and then runs `run/run_cond_vae.sh canny` on GPU 0 and `run/run_cond_vae.sh depth` on GPU 1 (6000 steps, batch 8; ~2 h each; markers `done_condvae_canny.txt` / `done_condvae_depth.txt`; outputs `/data/wookiekim/cgd/data/cond_vae/<cond>/{best,last}.safetensors`, decodes `outputs/omini/<cond>/condvae/`, records `omini.condvae@512.<cond>.json`). All three controller blocks finished earlier (`done_omini_block.txt`, `done_fluxcn_block.txt` 02:22 KST, `done_easycontrol_block.txt` 05:42 KST) and are filled in the paper. For future blocks (seg / bbox LoRAs): `run/run_block.sh <ctrl> "<conds>"` with `GPUS`; every stage skips existing outputs, so re-running the same command resumes it. A block takes ~13 h on two GPUs (three scoring chains) and ~8 h on four.

## 4. Remaining work (method-independent unless stated)

| # | Task | Needs | Fills |
|---|---|---|---|
| 1 | All three controller blocks filled (2026-09-12). Open: which controller feeds the seg group of tab:recon (row 11 below; EasyControl rows sit in a `%` comment) | user decision | tab:recon seg group |
| 2 | Train the OPTIONAL OminiControl seg LoRA (the bbox LoRAs were dropped 2026-09-12 with the layout condition) with the official recipe on `train/ade20k_train` (drop `blocked`, use `split`), then their latent caches + blocks | ~1 GPU-day each | seg / layout cells of the controller blocks |
| 3 | Conditioned VAE decoder (the 2x2 archetype): train on the OminiControl latents + conditions, score with the harness | small training job | tab:decoder-conditioning |
| 4 | CGD training triplets (`make_targets.py --set multigen_train30` and `--set ade20k_train`; `coco_train` dropped 2026-09-12) | ~20 GPU-hours (1.3 s/row measured; ~5 h on 4 GPUs) | adapter training data |
| 5 | Condition-scale sweep for fig:ceiling on dev-200 (`condition_scale` in OminiControl's `generate`) | ~1 GPU-hour | fig:ceiling |
| 6 | Eyeball the lifted numbers of tab:contextual / tab:subject against the official PDFs | reading | supplementary |
| 7 | **CGD rows** (every table) and the choice of K (16 or 24; the truncation sweep informs it) | THE METHOD | |
| 8 | Subject track (DreamBench, released subject LoRAs) | low priority | tab:subject |
| 9 | 1024 side comparison (QUEUED, not launched): EasyControl + FLUX ControlNet generated at 1024, VAE-decoded, downsampled to 512, scored on `subset500` (tests the 512-resolution caveat; side comparison only) | ~4 GPU-hours, after the blocks | supplementary table |
| 11 | DECIDED 2026-09-12: the seg group of the paper's reconstruction table uses EasyControl's released seg LoRA now (filled, red-flagged in the paper). Training an OminiControl seg LoRA (task 2) stays optional for a single controller across tables; it would also fill the OminiControl seg cells of tab:main and the 2x2 | optional GPU-day | tab:main / 2x2 OminiControl seg cells |
| 10 | Checkpoint consistency: vanilla PiD rows use the 2K checkpoint, a trained CGD will be v1.5-based (only undistilled FLUX checkpoint); re-decode vanilla rows with v1.5 distilled (~8 GPU-hours per block) or keep 2K as headline; decide with the method | decision | tab:main PiD rows |

Rules that bit us: never let two generator processes write the same set without disjoint shards (a race produced six corrupt PNGs, since regenerated); every decoder reads the cached latents, never regenerates; the harness refuses to upsample.

**2026-09-12 night (user): queued chain after the teacher block.** `/data/wookiekim/cgd/tmp/after_teacher_chain.sh` (detached in the container) builds the `div8k1k` split on CPU right away (`build_div8k1k.py --build`, from the DIV8K zip in the HF cache; LSDIR was the user's first choice but its HR images are ~1K on the short side, none >= 2048; marker `done_hires_build.txt`), waits for `done_teacher_block.txt`, then runs `run/run_hires_validation.sh` on GPUs 0-3 (captions, reference rows, OminiControl canny latents under `outputs/omini_div8k/`, PiD student decodes K = 28 / 24 / 16, three edge columns + daggers + FID; marker `done_hires_validation.txt`, ~2 GPU-h) and then `run/run_native_route.sh` (three controllers generating at 2048 + VAE on subset500, canny + depth, `outputs/<ctrl>_2048/`; marker `done_native_route.txt`, ~30 GPU-h). Status lines: `_logs/chain_status.txt`, `hires_status.txt`, `native_route_status.txt`. Fill = paper `tab:hires` (supplementary, red until filled) + the red native-route paragraph; `make_baselines.py` adds Section F2 for div8k1k automatically. Conditioned VAE decoder DONE 08:22 KST: canny F1 0.769 (VAE 0.771), depth RMSE 23.8 (23.8), pFID 19.1 (20.4); tab:decoder-conditioning row filled.

**2026-09-12 10:05 KST: side1024 DONE and filled** (`tab:side1024`): generating at 1024 lowers adherence for both controllers trained at 1024 (EasyControl canny F1 0.810 -> 0.793, depth RMSE 22.0 -> 24.4; FLUX ControlNet 0.646 -> 0.433, 29.2 -> 31.2 on subset500, same 512 condition, VAE decode, 512 view), so the 512 protocol stands (OPEN_QUESTIONS 11 resolved). Teacher block launched 09:53 KST on all four GPUs, sharing them with the four archetype runs (`condvae_{mod,ft}`) for their first ~1.5 h.

**2026-09-12 13:15 KST: all four archetype runs DONE** (`condvae_mod` canny / depth, `condvae_ft` canny / depth; markers `done_condvae_<cond>_<variant>.txt`). Modulated: canny F1 0.766 (strict 0.442), depth RMSE 23.8, pFID 18.9; finetuned retry (lr 1e-5) 0.770 / 23.8, stable. Both agree with the additive form: a deterministic decoder does not gain tolerant adherence from the condition. Paper tab:decoder-conditioning carries the additive and modulated rows; the ft run is in the provenance comment. Teacher block: phase 1 decodes finished 13:17 KST (well ahead of the estimate), dev-200 scoring running.

**2026-09-12 13:29 KST: dev-200 FINAL table filled** (README auto-block; BASELINES Section E; DEV_SETTING snapshot). Teacher rows: final 0.7636 / 0.7114 / 0.4807, K=24 0.6938 / 0.6654 / 0.4427, K=16 0.5194 / 0.5296 / 0.3562 (17.5 s/img, 21.5 GB). Native route (OminiControl generating at 2048 + VAE): COLLAPSED (F1 0.04, MUSIQ 35; sane at 1024), documented in the README. Teacher block PHASE 2 (subset500) running on all four GPUs since 13:29.

**2026-09-12 20:10 KST: teacher block DONE and filled** (`tab:teacher`, efficiency teacher row, BASELINES F). Student vs teacher on subset500: K=28 0.783 / 0.733 / 0.544 vs 0.769 / 0.720 / 0.496; K=24 0.716 / 0.669 / 0.510 vs 0.695 / 0.675 / 0.458; K=16 0.546 / 0.571 / 0.432 vs 0.528 / 0.540 / 0.367 (512/c512, 2048/c512, 2048/c2048); depth RMSE 22.9 / 23.0 / 29.9 vs 23.2 / 23.6 / 29.6; teacher decode 12.7 s vs 1.0 s. Teacher round trip vs c2048 = 0.621 (c2048 is tied to the student decoder; the DIV8K validation checks it against real edges). **Bug fixed 18:25 KST:** `run_teacher_block.sh` had scored the canny subset500 rows on the depth-run decodes (bash `local` expansion of `$c`); canny rows rescored on GPUs 0-1 (`tmp/rescore_teacher_canny.sh`); depth / round trips / efficiency were unaffected; `run_native_route.sh` fixed before it runs; the controller-block runners use `$2` and are fine. DIV8K validation: scoring since 19:29 KST.

**2026-09-13 03:30 KST: native route scored early for EasyControl + FLUX ControlNet** (OminiControl generating until ~14:00 KST): at 2048 generation the adherence collapses (EasyControl canny F1 0.258 at the 512 view vs 0.810 at 512 generation, depth RMSE 49.9 vs 22.0; ControlNet 0.163 vs 0.646, 35.3 vs 29.2), MUSIQ@2048 64.6 / 61.7, 36 s/img. Paper: `tab:native-route` (supplementary), red text removed; BASELINES A2. Paired PiD K=24 + dagger rows on subset500 scored by `tmp/score_native_pairs.sh`.

**2026-09-13 11:12 KST: native route COMPLETE** (OminiControl at 2048: F1 0.042 / 0.059 / 0.032, depth RMSE 59.7, MUSIQ 56, FID 240 = collapse). The whole 2026-09-12 queue (side1024, condvae x4, dev-200 final table, teacher block, DIV8K validation, native route) is done and filled; all four GPUs free. Next GPU work is the CGD method itself (`eval_variant.py` gate) and, when the user gives the go-ahead, the CGD training targets.

**2026-09-13 13:35 KST: new queue (user order 1-2-5).** (1) OminiControl seg adapter training on GPU 0 (`run/run_omini_seg.sh`; official recipe; then the seg block + condvae seg; marker `done_item1_omini_seg.txt`). (2) Re-noised archetype `condvae_rn` / `condvae_rn_mod` on GPUs 1-2 (decoded at x0 and x_t@24 / @16; marker `done_item2_condvae_rn.txt`). (5) Teacher decodes for EasyControl + ControlNet on subset500 (GPU 3 now, GPUs 1-2 after item 2; marker `done_item5_teacher_ctrls.txt`). Container: lightning / prodigyopt installed with --no-deps (torch unchanged).

**2026-09-14 00:40 KST: OminiControl seg block DONE and filled** (our adapter, 9000 optimizer steps): mIoU 43.9 (VAE) / 43.4 (PiD K=24) vs EasyControl's released 39.3 / 39.6; FID 30.1. The paper's segmentation rows are now all under OminiControl; red stand-in texts removed. Archetype seg cells running (condvae seg add / mod). Teacher decodes for EasyControl / ControlNet: shard 0 done 20:34, shards 1-2 running.

**Timestamps:** the container and `_logs/` markers are UTC (`date` -> Etc/UTC). Times labelled KST in the 2026-09-12/13 entries above are server UTC; add 9 h for KST.

**2026-09-14 06:45 UTC: item 5 DONE** (teacher rows for EasyControl + FLUX ControlNet on subset500). Teacher within 0.01 of the student at the matched view; 0.05 to 0.07 below at 2048 vs c512; 0.11 to 0.13 below vs c2048 (the c2048 map is the student's round trip). Depth within 0.5 RMSE. The 1-2-5 queue is complete; GPU 0 builds the CGD training targets (`tmp/targets_chain.sh`), GPUs 1-2 score the student K=28 / 16 subset rows for the two controllers, GPU 3 idle.

**2026-09-14 20:00 UTC: everything queued is done; all four GPUs idle.** CGD training targets complete (multigen_train30 36,622; ade20k_train 20,201; `train/<set>/{targets,latents,conditions}`, `TARGETS_VERSION.json`). Decoder-seed variance: PiD K=24 with seeds 1 and 2 on subset500 moves F1 vs c512 by <= 0.002 and depth RMSE by <= 0.3 (records `omini.pid_s{1,2}_k24@*.subset500*`). Caveat found: the 2048 / c2048 column rewards the decoder seed the reference round trip used (0.510 at seed 0 vs 0.483 / 0.481 at seeds 1 / 2); a fix (rebuild `canny2048` from an unused seed and rescore that column) is proposed, not run. Student K=28 / 16 subset rows for EasyControl / ControlNet added (BASELINES F).
